import React from 'react'
import {Typography} from "@mui/material";

export default function Teams() {
    return (
    <div>
        <Typography
            variant="h1"
            color="primary">
            List of Teams
        </Typography>
    </div>
    )
}