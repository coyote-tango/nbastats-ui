import React from 'react'
import {Typography} from "@mui/material";

export default function Players() {
    return (
    <div>
        <Typography
            variant="h1"
            color="primary">
            List of players
        </Typography>
    </div>
    )
}